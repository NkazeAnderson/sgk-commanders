// Drizzle configuration removed — this project is migrating away from Drizzle.
// Migrations are now managed with the Supabase CLI and stored in `apps/mobile/supabase/migrations/`.
// If you need to run or inspect Drizzle config, consider restoring history from git.

export default {} as any;
