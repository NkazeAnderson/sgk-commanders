// dbSchema and Drizzle-specific table definitions have been removed.
// This repository uses Supabase migrations now (stored in `apps/mobile/supabase/migrations/`).
// If you need a reference model, use the Zod schemas in `packages/shared/src/zodSchema.ts` which mirror the DB.

